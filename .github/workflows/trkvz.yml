name: Secure Video Token Updater

on:
  schedule:
    # Token'ların süresi dolduğu için her saat başı otomatik çalışır
    - cron: '0 * * * *'
  workflow_dispatch: # Manuel çalıştırma butonu

permissions:
  contents: write

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Repoyu Çek
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Bağımlılıkları Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Token Betiğini Çalıştır
        run: python main.py

      - name: Değişiklikleri Kaydet ve Push Et
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add trkvz-tokens.txt
          git diff --staged --quiet || (git commit -m "Auto update secure video tokens [skip ci]" && git push)
